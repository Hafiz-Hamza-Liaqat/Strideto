import { useEffect,useState } from 'react';import { agentApi } from '../../services/agentService';
export default function AgentCommerce(){const [data,setData]=useState({orders:[],transactions:[]});useEffect(()=>{agentApi.getCommerceHistory().then(r=>setData(r.data));},[]);return <div className="space-y-6"><header><h1 className="text-2xl font-semibold">Commerce</h1><p className="mt-1 text-sm text-slate-600">Payments and payouts are not enabled yet. Draft orders are not earnings or settled funds.</p></header><section><h2 className="font-semibold">Orders</h2>{data.orders.length?data.orders.map(x=><p key={x._id} className="mt-2 rounded border bg-white p-3">{x.orderNumber} · {x.currency} {x.amountMinor} minor units · {x.status}</p>):<p className="mt-2 text-sm text-slate-500">No orders.</p>}</section><section><h2 className="font-semibold">Transactions</h2>{data.transactions.length?data.transactions.map(x=><p key={x._id} className="mt-2 rounded border bg-white p-3">{x.type} · {x.status}</p>):<p className="mt-2 text-sm text-slate-500">No confirmed transactions.</p>}</section></div>}
