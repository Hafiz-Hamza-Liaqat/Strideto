/**
 * Canonical CURRENT eligibility adapter (Phase 17D-2). No public route.
 */
export { resolvePublicationEligibility, isCurrentPublicEligible } from '../../../../shared/gbs/publicationEligibility.js';
