import mongoose from 'mongoose';
const schema = new mongoose.Schema({ recipientActorType: { type: String, enum: ['student', 'agent', 'staff'], required: true }, recipientId: { type: String, required: true }, eventType: { type: String, enum: ['review_received', 'agent_response', 'report_status_update', 'dispute_opened', 'dispute_response_needed', 'dispute_resolution', 'moderation_action'], required: true }, entityType: String, entityId: mongoose.Schema.Types.ObjectId, status: { type: String, enum: ['pending'], default: 'pending', immutable: true }, deliveryAttempted: { type: Boolean, default: false, immutable: true } }, { timestamps: true, collection: 'trust_notification_events' });
export const TrustNotificationEvent = mongoose.models.TrustNotificationEvent || mongoose.model('TrustNotificationEvent', schema);
